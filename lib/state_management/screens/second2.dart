import 'package:flutter/material.dart';

class StateSecond extends StatelessWidget {
  const StateSecond({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('slider'),
      ),
    );
  }
}
